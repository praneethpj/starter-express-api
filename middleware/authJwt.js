const jwt = require("jsonwebtoken");
const config = require("../config/auth.config.js");
const db = require("../models");
const User = db.user;

verifyToken = (req, res, next) => {
  let token = req.headers["x-access-token"];

  if (!token) {
    return res.status(403).send({
      message: "No token provided!"
    });
  }

  jwt.verify(token, config.secret, (err, decoded) => {
    if (err) {
      return res.status(401).send({
        message: "Unauthorized!"
      });
    }
    req.userId = decoded.id;
    next();
  });
};

isAdmin = (req, res, next) => {
  User.findByPk(req.userId).then(user => {
    user.getRoles().then(roles => {
      for (let i = 0; i < roles.length; i++) {
        if (roles[i].name === "admin") {
          next();
          return;
        }
      }

      res.status(403).send({
        message: "Require Admin Role!"
      });
      return;
    });
  });
};

isModerator = (req, res, next) => {
  User.findByPk(req.userId).then(user => {
    user.getRoles().then(roles => {
      for (let i = 0; i < roles.length; i++) {
        if (roles[i].name === "moderator") {
          next();
          return;
        }
      }

      res.status(403).send({
        message: "Require Moderator Role!"
      });
    });
  });
};

isModeratorOrAdmin = (req, res, next) => {
  User.findByPk(req.userId).then(user => {
    user.getRoles().then(roles => {
      for (let i = 0; i < roles.length; i++) {
        if (roles[i].name === "moderator") {
          next();
          return;
        }

        if (roles[i].name === "admin") {
          next();
          return;
        }
      }

      res.status(403).send({
        message: "Require Moderator or Admin Role!"
      });
    });
  });
};

isSubscruptionKeyActive = (req, res, next) => {
  let token = req.headers["x-access-token"];
  let userid="";
  if (!token) {
    return res.status(403).send({
      message: "No token provided!"
    });
  }

  jwt.verify(token, config.secret, (err, decoded) => {
    if (err) {
      return res.status(401).send({
        message: "Unauthorized!"
      });
    }
    userid = decoded.id;
    
  });

  User.findByPk(userid).then(user => {
    if(user.subscriptionkey!=req.body.subscriptionkey){
      res.status(403).send({
        message: "Your subscription key is invalided"
      });
    }
    
    
  });
  next();
};

isPaymentActive = (req, res, next) => {
  let token = req.headers["x-access-token"];
  let userid="";
  if (!token) {
    return res.status(403).send({
      message: "No token provided!"
    });
  }

  jwt.verify(token, config.secret, (err, decoded) => {
    if (err) {
      return res.status(401).send({
        message: "Unauthorized!"
      });
    }
    userid = decoded.id;
    
  });

  User.findByPk(userid).then(user => {
    if(user.paymentactive!=1){
      res.status(403).send({
        message: "Your subscription is invalided"
      });
    }
    
    
  });
  next();
};


isUserActive = (req, res, next) => {
  let token = req.headers["x-access-token"];
  let userid="";
  if (!token) {
    return res.status(403).send({
      message: "No token provided!"
    });
  }

  jwt.verify(token, config.secret, (err, decoded) => {
    if (err) {
      return res.status(401).send({
        message: "Unauthorized!"
      });
    }
    userid = decoded.id;
    
  });

  User.findByPk(userid).then(user => {
    if(user.useractive!=1){
      res.status(403).send({
        message: "user is not activated"
      });
    }
    
    
  });
  next();
};

const authJwt = {
  verifyToken: verifyToken,
  isAdmin: isAdmin,
  isModerator: isModerator,
  isModeratorOrAdmin: isModeratorOrAdmin,
  isPaymentActive:isPaymentActive,
  isUserActive:isUserActive,
};
module.exports = authJwt;